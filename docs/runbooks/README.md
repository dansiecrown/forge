# Operational runbooks

This directory holds operational procedures such as queue replay, restore, incident handling, and retention execution, as defined in `docs/project-structure.md`.

No runbooks exist yet because the underlying capabilities (queues, backups, retention jobs, incident tooling) are not built until later roadmap phases (`docs/development-roadmap.md`, Phase 2 onward). Add a runbook alongside the capability it documents.
