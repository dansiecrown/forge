name: forge-dev

# Local development infrastructure only — never used for staging/production,
# which use managed services per docs/system-architecture.md §14. Redis is
# used by the API for Fellowship chat's real-time event fan-out
# (docs/adr/0014-fellowship-chat.md) — never as the source of truth for
# messages or authorization, and the API degrades gracefully (persisted
# writes keep working, only live delivery is affected) if it's unreachable.

services:
  postgres:
    image: postgres:16-alpine
    container_name: forge-postgres
    restart: unless-stopped
    environment:
      POSTGRES_USER: forge
      POSTGRES_PASSWORD: forge
      POSTGRES_DB: forge
    ports:
      - '5432:5432'
    volumes:
      - forge_postgres_data:/var/lib/postgresql/data
    networks:
      - forge-dev
    healthcheck:
      test: ['CMD-SHELL', 'pg_isready -U forge -d forge']
      interval: 5s
      timeout: 5s
      retries: 10

  redis:
    image: redis:7-alpine
    container_name: forge-redis
    restart: unless-stopped
    ports:
      - '6379:6379'
    volumes:
      - forge_redis_data:/data
    networks:
      - forge-dev
    healthcheck:
      test: ['CMD', 'redis-cli', 'ping']
      interval: 5s
      timeout: 5s
      retries: 10

volumes:
  forge_postgres_data:
  forge_redis_data:

networks:
  forge-dev:
    driver: bridge
